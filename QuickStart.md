# CopilotKit QuickStart Guide

This guide documents the setup steps for integrating CopilotKit with Local LM Studio into your Next.js application.

## Step 1: Configure Environment Variables

Create a `.env` file in the root directory with your LM Studio configuration:

```env
# LM Studio Configuration
LM_STUDIO_URL=http://127.0.0.1:1234
LM_STUDIO_MODEL=gemma-3-27b-it/gemma-3-27b-it-Q4_K_M.gguf
LM_STUDIO_EMBEDDING=text-embedding-nomic-embed-text-v1.5-embedding/nomic-embed-text-v1.5.f16.gguf
```

## Step 2: Setup CopilotKit Runtime with LM Studio

Create an API route at `src/app/api/copilotkit/route.ts`:

```typescript
import {
  CopilotRuntime,
  copilotRuntimeNextJSAppRouterEndpoint,
  OpenAIAdapter,
} from "@copilotkit/runtime";
import { BuiltInAgent } from "@copilotkit/runtime/v2"; 
import { NextRequest } from "next/server";

// Configure LM Studio adapter (OpenAI-compatible)
const serviceAdapter = new OpenAIAdapter({
  baseURL: "http://127.0.0.1:1234/v1",
  apiKey: "lm-studio", // LM Studio doesn't require a real key
  model: "gemma-3-27b-it/gemma-3-27b-it-Q4_K_M.gguf",
});

// Configure embedding adapter for Nomic
const embeddingAdapter = new OpenAIAdapter({
  baseURL: "http://127.0.0.1:1234/v1",
  apiKey: "lm-studio",
  model: "text-embedding-nomic-embed-text-v1.5-embedding/nomic-embed-text-v1.5.f16.gguf",
});

const builtInAgent = new BuiltInAgent({ 
  model: "gemma-3-27b-it/gemma-3-27b-it-Q4_K_M.gguf",
  adapter: serviceAdapter,
});

const runtime = new CopilotRuntime({
  agents: { default: builtInAgent },
});

export const POST = async (req: NextRequest) => {
  const { handleRequest } = copilotRuntimeNextJSAppRouterEndpoint({
    runtime,
    endpoint: "/api/copilotkit",
  });

  return handleRequest(req);
};
```

## Step 3: Configure CopilotKit Provider

Update `src/app/layout.tsx` to wrap your application with the CopilotKit provider:

```typescript
import type { Metadata } from "next";
import { Geist, Geist_Mono } from "next/font/google";
import { CopilotKit } from "@copilotkit/react-core";
import "@copilotkit/react-ui/v2/styles.css";
import "./globals.css";

const geistSans = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
});

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
});

export const metadata: Metadata = {
  title: "Create Next App",
  description: "Generated by create next app",
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html
      lang="en"
      className={`${geistSans.variable} ${geistMono.variable} h-full antialiased`}
    >
      <body className="min-h-full flex flex-col">
        <CopilotKit runtimeUrl="/api/copilotkit">
          {children}
        </CopilotKit>
      </body>
    </html>
  );
}
```

## Step 4: Add the Chat Interface

Update `src/app/page.tsx` to include the CopilotSidebar component:

```typescript
import { CopilotSidebar } from "@copilotkit/react-core/v2";

export default function Page() {
  return (
    <main>
      <h1>Your App</h1>
      <CopilotSidebar />
    </main>
  );
}
```

## Step 5: Start LM Studio

**Important**: Before starting the development server, ensure LM Studio is running with the models loaded:

1. Open LM Studio
2. Load the models:
   - **LLM Model**: `gemma-3-27b-it/gemma-3-27b-it-Q4_K_M.gguf`
   - **Embedding Model**: `text-embedding-nomic-embed-text-v1.5-embedding/nomic-embed-text-v1.5.f16.gguf`
3. Start the local server (should be running on `http://127.0.0.1:1234`)

## Step 6: Start the Development Server

Run the development server:

```bash
npm run dev
```

Your CopilotKit-powered application should now be running at `http://localhost:3111`!

## What's Configured

- ✅ Local LM Studio integration (bypasses network restrictions)
- ✅ Gemma 3 27B model for chat responses
- ✅ Nomic Embed text embeddings
- ✅ CopilotKit runtime with BuiltInAgent
- ✅ CopilotKit provider wrapping the application
- ✅ CopilotSidebar chat interface added to the main page

## Troubleshooting

- **Forbidden Error**: Ensure LM Studio is running and the server is started
- **Connection Error**: Verify LM Studio is accessible at `http://127.0.0.1:1234`
- **Model Not Found**: Confirm both models are loaded in LM Studio

## Next Steps

- Customize the chat interface
- Add custom agents and actions
- Integrate with your application logic
- Switch between different local models in LM Studio
